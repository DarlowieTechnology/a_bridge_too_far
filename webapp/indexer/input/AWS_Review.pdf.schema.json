{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWS_Review.pdf Security Finding",
  "type": "object",
  "properties": {
    "identifier": {
      "type": "string",
      "description": "The unique reference identifier for the finding."
    },
    "title": {
      "type": "string",
      "description": "The title or name of the security issue."
    },
    "risk_rating": {
      "type": "string",
      "description": "The severity level of the finding.",
      "enum": [
        "high",
        "medium",
        "low"
      ]
    },
    "status": {
      "type": "string",
      "description": "The current status of the finding.",
      "enum": [
        "open"
      ]
    },
    "description": {
      "type": "string",
      "description": "A detailed explanation of the security issue discovered."
    },
    "recommendation": {
      "type": "string",
      "description": "The suggested actions to remediate the identified risk."
    },
    "affects": {
      "type": "string",
      "description": "The specific resource or environment impacted by the finding."
    }
  },
  "required": [
    "identifier",
    "title",
    "risk_rating",
    "status",
    "description",
    "recommendation",
    "affects"
  ]
}